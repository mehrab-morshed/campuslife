config = {
  'magnitude_threshold'   : 9.6,    # only consider magnitudes above this threshold
  'delta_threshold'       : 0.03   # only consider entries where the
                                    # difference between the current and next
                                    # entry magnitude is equal or above
                                    # this threshold
}
